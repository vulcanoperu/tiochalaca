/**
 * espnAdapter.js — Re-export de adapters/espnAdapter.js (compatibilidad)
 *
 * ⚠️ DEPRECADO: Usar require('./adapters/espnAdapter') en código nuevo.
 * Este archivo se mantiene para no romper imports existentes.
 */
module.exports = require('./adapters/espnAdapter');
